# Source data and intermediates: all regenerable from the pipeline, and far too
# large for a repository. `python build/extract2.py` onward rebuilds everything.
data/

# Generated bundles and graph slices, built on demand; the site itself is the
# committed root index.html plus one hashed graph-web-*.json.gz asset.
web/graph.json
web/graph-web.json
web/graph-full.json
web/index.html
web/index-web.html
web/index-full.html
web/trees.json

recon/*.json
__pycache__/
*.pyc
